package com.google.protobuf;

import java.io.InputStream;

/* compiled from: Parser */
public interface aj<MessageType> {
    MessageType b(h hVar);

    MessageType b(h hVar, q qVar);

    MessageType b(byte[] bArr);

    MessageType b(byte[] bArr, q qVar);

    MessageType c(g gVar, q qVar);

    MessageType c(InputStream inputStream);

    MessageType d(h hVar, q qVar);

    MessageType d(InputStream inputStream);

    MessageType e(InputStream inputStream, q qVar);

    MessageType f(InputStream inputStream, q qVar);
}
